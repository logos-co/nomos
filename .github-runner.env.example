# Copy this file to .github-runner.env and adjust the values before running
# ./scripts/actions-runner-docker.sh

# Required: GitHub repository or organization URL and a short-lived token generated
# from Settings → Actions → Runners → New self-hosted runner.
RUNNER_URL=https://github.com/ORG/REPO
RUNNER_TOKEN=REPLACE_ME

# Optional runner metadata that appears in the GitHub UI.
RUNNER_NAME=mac-docker
RUNNER_LABELS=linux,self-hosted,nomos
RUNNER_GROUP=Default

# Container/image configuration.
RUNNER_IMAGE=ghcr.io/actions/actions-runner:latest
RUNNER_HOME=$HOME/.cache/nomos-actions-runner
RUNNER_CONTAINER_NAME=nomos-actions-runner
RUNNER_WORKDIR=_work

# Mount the host Docker socket so jobs can run `docker build` inside the runner.
RUNNER_DOCKER_SOCKET=/var/run/docker.sock
# The helper script auto-detects the socket group, relaxes its permissions, and on macOS
# also joins the root group so Docker Desktop's VM socket is writable.
# RUNNER_DOCKER_SOCKET_GROUP_ID=

# Optional comma-separated list of extra mounts passed directly to docker (`host:container[:mode]`).
# Example: RUNNER_EXTRA_MOUNTS=/tmp/cache:/cache:rw,/opt/toolchains:/toolchains:ro
# RUNNER_EXTRA_MOUNTS=

# Advanced toggles.
RUNNER_REPLACE_EXISTING=false
RUNNER_DISABLE_UPDATES=false
